import React from "react";
import { Typography } from "@material-ui/core";

const SideBar = () => {
    return (
        <>
            <Typography variant="h5">Side bar</Typography>
            <p>Sidebar is statring from here</p>
        </>
    );
};

export default SideBar;
